{
  "name": "claw",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:prompts": "node scripts/generate-claw-prompts.mjs",
    "predev": "npm run generate:prompts",
    "dev": "wrangler dev",
    "predeploy": "npm run generate:prompts",
    "deploy": "wrangler deploy",
    "pretest": "npm run generate:prompts",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "4.114.0"
  }
}
